# The sample app under test for the E2E/UI track.
# Start with:  docker compose -f docker/webapp/docker-compose.yml up
services:
  webapp:
    image: python:3.11-slim
    working_dir: /app
    volumes:
      - ./:/app
    command: python app.py
    ports:
      - "8080:8080"
