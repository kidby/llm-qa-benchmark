# Flow: Log in with valid credentials

1. Open the home page (the login form).
2. Fill the field labelled "Username" with `demo`.
3. Fill the field labelled "Password" with `password123`.
4. Click the "Log in" button.
5. The app navigates to a welcome screen.

Success: a heading containing the text "Welcome, demo" is visible.
